@page
@model IndexModel
@{
	ViewData["Title"] = "Магазин освещения";
}

<div class="col-12 mb-per-15 pt-per-5" data-color-section="dark">
	@if (@Model.DisplayAboutPages.Count() > 0)
	{
		@foreach (var aboutPage in @Model.DisplayAboutPages.Where(c => c.DisplayHomePage == true))
		{
			<div class="scroll-m row justify-content-between align-items-xl-center">
				<div class="col-12 col-xl-6">
					<div class="black-img__wrapper">
						<div class="black-img">
							<h3 class="black-img-h">Lights 4 Apart</h3>
							<h4 class="black-img-p">WE CREATE LIGHT</h4>
						</div>
					</div>
				</div>
				<div class="col-12 col-xl-5 col-xs-12 hook">
					<h1 class="p__caption mb-per-5">@aboutPage.HeadingOneNode</h1>
					<a asp-page="About" asp-route-ID="@aboutPage.ID" class="a-btn-link">Узнать больше</a>
				</div>
			</div>
		}
	}
</div>

<div id="footer-bg" class="col-12">
	@if (@Model.DisplayCategory.Count() > 0)
	{
		int categoryNum = 0;
		@foreach (var category in @Model.DisplayCategory.Where(c => c.DisplayHomePage == true))
		{
			@if (categoryNum % 2 == 0)
			{
				<div class="scroll-m row justify-content-between align-items-xl-center mb-per-10">
					<div class="col-12 col-xl-5 col-xs-12 order-block-2 hook">
						<h1 class="h1__caption mb-per-5">@category.CategoryName</h1>
						<p class="p__caption mb-per-5">@category.Description</p>
						<a asp-page="CategoryPage" asp-route-CategoryID="@category.ID" class="a-btn-link">Узнать больше</a>
					</div>
					<div class="col-12 col-xl-6 order-block-1">
						<div class="img-center__wrapper">
							<img class="img-responsive" src="@category.CategoryImg">
						</div>
					</div>
				</div>
				categoryNum++;
			}
			else
			{
				<div class="scroll-m row justify-content-between align-items-xl-center mb-per-10">
					<div class="col-12 col-xl-6 order-block-1">
						<div class="img-center__wrapper">
							<img class="img-responsive" src="@category.CategoryImg">
						</div>
					</div>
					<div class="col-12 col-xl-5 col-xs-12 order-block-2 hook">
						<h1 class="h1__caption mb-per-5">@category.CategoryName</h1>
						<p class="p__caption mb-per-5">@category.Description</p>
						<a asp-page="CategoryPage" asp-route-CategoryID="@category.ID" class="a-btn-link">Узнать больше</a>
					</div>
				</div>
				categoryNum++;
			}

		}
	}
</div>
